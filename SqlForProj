DROP DATABASE IF EXISTS foodSQL;
CREATE DATABASE IF NOT EXISTS foodSQL;
USE foodSQL;


SELECT 'CREATING TABLES' as 'INFO';


DROP TABLE IF EXISTS food;
CREATE TABLE carb (
	Name VARCHAR(30) NOT NULL,
	calpergram FLOAT NOT NULL,
	PRIMARY KEY(Name) );
SELECT 'INSERTING DATA INTO TABLE CARB' as 'INFO';
INSERT INTO food VALUES ('White Bread', 2.65);
INSERT INTO food VALUES ('Steak', 2.71);
INSERT INTO food VALUES ('Cheese', 4.02);
SELECT 'CHECKING TO SEE IF DATA INSERTED TO CARBS CORRECTLY' as 'INFO';
SELECT * FROM food;
